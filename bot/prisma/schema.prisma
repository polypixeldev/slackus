generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model App {
  id            String   @id @default(cuid())
  user          String
  bot           String
  command       String
  interval      Int
  conversations String
  lastCheck     DateTime?
}
